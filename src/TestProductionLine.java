public class TestProductionLine extends EmployeeInfo {
  
  /**
   * This will create an object of EmployeeInfo (data for an individual employee) and then access &
   * display those class variables.
   *
   * @param args : Standard args for main.
   */
  public static void main(String[] args) {
    EmployeeInfo employeeObj = new EmployeeInfo();
    employeeObj.getCode();
    employeeObj.toString();
    
  }
  
}
